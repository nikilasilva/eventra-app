import React from 'react';
import {Text, View} from "react-native";

const TicketsScreen = () => {
    return (
        <View>
            <Text className="text-white font-pjsmedium text-xs">Tickets Screen</Text>
        </View>
    );
};

export default TicketsScreen;
