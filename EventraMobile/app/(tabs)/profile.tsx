import React from "react";
import {Text, View} from 'react-native';

const ProfileScreen = () => {
    return (
        <View>
            <Text className="text-white font-pjsmedium text-xs">Profile Screen</Text>
        </View>
    );
};

export default ProfileScreen;